---
# Read only the effective build definitions; runtime environment and mounts
# must not trigger image builds. Keep resolved configuration out of logs.
- name: Inspect local build inputs
  when: not ansible_check_mode
  no_log: true
  block:
    - name: Read effective Compose configuration
      ansible.builtin.command:
        argv: [docker, compose, config, --format, json]
        chdir: "{{ docker_base_path }}/{{ stack_name }}"
      register: strudel_build_compose
      changed_when: false

    - name: Reset build service list
      ansible.builtin.set_fact:
        strudel_build_services: []

    - name: Select local build definitions
      ansible.builtin.set_fact:
        strudel_build_services: >-
          {{ strudel_build_services + [{
            'name': item.key,
            'image': item.value.image | default(
              (strudel_build_compose.stdout | from_json).name ~ '-' ~ item.key),
            'platform': item.value.platform | default(''),
            'build': item.value.build
          }] }}
      loop: "{{ (strudel_build_compose.stdout | from_json).services | dict2items }}"
      when: item.value.build is defined

    - name: Check local build images
      community.docker.docker_image_info:
        name: "{{ item.image }}"
      loop: "{{ strudel_build_services }}"
      register: strudel_build_images

    - name: Hash build Dockerfiles
      ansible.builtin.stat:
        path: >-
          {{ [item.build.context, item.build.dockerfile | default('Dockerfile')] | path_join }}
        checksum_algorithm: sha256
      loop: "{{ strudel_build_services }}"
      when: item.build.dockerfile_inline is not defined
      register: strudel_build_dockerfiles

    - name: Hash build ignore files
      ansible.builtin.stat:
        path: "{{ item.build.context }}/.dockerignore"
        checksum_algorithm: sha256
      loop: "{{ strudel_build_services }}"
      register: strudel_build_ignore

    - name: Hash Dockerfile-specific ignore files
      ansible.builtin.stat:
        path: >-
          {{ ([item.build.context, item.build.dockerfile | default('Dockerfile')] | path_join) ~ '.dockerignore' }}
        checksum_algorithm: sha256
      loop: "{{ strudel_build_services }}"
      register: strudel_build_dockerfile_ignore

    - name: Calculate build fingerprint
      ansible.builtin.set_fact:
        strudel_build_fingerprint: >-
          {{ {
            'version': 1,
            'source': '',
            'services': strudel_build_services,
            'dockerfiles': strudel_build_dockerfiles.results | map(attribute='stat.checksum', default='') | list,
            'ignore': strudel_build_ignore.results | map(attribute='stat.checksum', default='') | list,
            'dockerfile_ignore': strudel_build_dockerfile_ignore.results | map(attribute='stat.checksum', default='') | list
          } | to_json(sort_keys=True) | hash('sha256') }}

    - name: Check for previous successful build inputs
      ansible.builtin.stat:
        path: "{{ docker_base_path }}/{{ stack_name }}/.ansible-build-inputs.json"
      register: strudel_build_record

    - name: Read previous successful build inputs
      ansible.builtin.slurp:
        src: "{{ docker_base_path }}/{{ stack_name }}/.ansible-build-inputs.json"
      when: strudel_build_record.stat.exists
      register: strudel_build_previous

    - name: Decide whether local images need building
      ansible.builtin.set_fact:
        strudel_build_required: >-
          {{ strudel_build_services | length > 0 and (
            strudel_force_rebuild | bool or
            (strudel_build_images.results | selectattr('images', 'equalto', []) | list | length > 0) or
            ((strudel_build_previous.content | default('e30=') | b64decode | from_json).fingerprint
              | default('')) != strudel_build_fingerprint
          ) }}
