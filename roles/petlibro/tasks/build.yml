---
# Read only the effective build definitions; runtime environment and mounts
# must not trigger image builds. Keep resolved configuration out of logs.
- name: Inspect local build inputs
  when: not ansible_check_mode
  no_log: true
  block:
    - name: Read effective Compose configuration
      ansible.builtin.command:
        argv: [docker, compose, config, --format, json]
        chdir: "{{ config_base }}"
      register: petlibro_build_compose
      changed_when: false

    - name: Reset build service list
      ansible.builtin.set_fact:
        petlibro_build_services: []

    - name: Select local build definitions
      ansible.builtin.set_fact:
        petlibro_build_services: >-
          {{ petlibro_build_services + [{
            'name': item.key,
            'image': item.value.image | default(
              (petlibro_build_compose.stdout | from_json).name ~ '-' ~ item.key),
            'platform': item.value.platform | default(''),
            'build': item.value.build
          }] }}
      loop: "{{ (petlibro_build_compose.stdout | from_json).services | dict2items }}"
      when: item.value.build is defined

    - name: Check local build images
      community.docker.docker_image_info:
        name: "{{ item.image }}"
      loop: "{{ petlibro_build_services }}"
      register: petlibro_build_images

    - name: Hash build Dockerfiles
      ansible.builtin.stat:
        path: >-
          {{ [item.build.context, item.build.dockerfile | default('Dockerfile')] | path_join }}
        checksum_algorithm: sha256
      loop: "{{ petlibro_build_services }}"
      when: item.build.dockerfile_inline is not defined
      register: petlibro_build_dockerfiles

    - name: Hash build ignore files
      ansible.builtin.stat:
        path: "{{ item.build.context }}/.dockerignore"
        checksum_algorithm: sha256
      loop: "{{ petlibro_build_services }}"
      register: petlibro_build_ignore

    - name: Hash Dockerfile-specific ignore files
      ansible.builtin.stat:
        path: >-
          {{ ([item.build.context, item.build.dockerfile | default('Dockerfile')] | path_join) ~ '.dockerignore' }}
        checksum_algorithm: sha256
      loop: "{{ petlibro_build_services }}"
      register: petlibro_build_dockerfile_ignore

    - name: Calculate build fingerprint
      ansible.builtin.set_fact:
        petlibro_build_fingerprint: >-
          {{ {
            'version': 1,
            'source': catbro_git.after | default(''),
            'services': petlibro_build_services,
            'dockerfiles': petlibro_build_dockerfiles.results | map(attribute='stat.checksum', default='') | list,
            'ignore': petlibro_build_ignore.results | map(attribute='stat.checksum', default='') | list,
            'dockerfile_ignore': petlibro_build_dockerfile_ignore.results | map(attribute='stat.checksum', default='') | list
          } | to_json(sort_keys=True) | hash('sha256') }}

    - name: Check for previous successful build inputs
      ansible.builtin.stat:
        path: "{{ config_base }}/.ansible-build-inputs.json"
      register: petlibro_build_record

    - name: Read previous successful build inputs
      ansible.builtin.slurp:
        src: "{{ config_base }}/.ansible-build-inputs.json"
      when: petlibro_build_record.stat.exists
      register: petlibro_build_previous

    - name: Decide whether local images need building
      ansible.builtin.set_fact:
        petlibro_build_required: >-
          {{ petlibro_build_services | length > 0 and (
            petlibro_force_rebuild | bool or
            (petlibro_build_images.results | selectattr('images', 'equalto', []) | list | length > 0) or
            ((petlibro_build_previous.content | default('e30=') | b64decode | from_json).fingerprint
              | default('')) != petlibro_build_fingerprint
          ) }}
