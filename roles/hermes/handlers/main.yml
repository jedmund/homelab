---
- name: Reload Hermes dashboard LaunchAgent
  ansible.builtin.shell: |
    launchctl bootout gui/$(id -u) {{ hermes_launchd_plist }} 2>/dev/null || true
    launchctl bootstrap gui/$(id -u) {{ hermes_launchd_plist }}
  become: false
  changed_when: true
