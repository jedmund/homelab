# {{ ansible_managed }}
# CI cache Garage process env. Cluster layout, bucket, and access key are
# NOT bootstrapped from here - see files/ci_cache_bootstrap.sh for the
# one-time (re-runnable) setup. Only the admin token lives here so the
# bootstrap script can authenticate against the admin API.

RUST_LOG=garage=info
GARAGE_ADMIN_TOKEN={{ gitlab_cache_garage_admin_token }}
