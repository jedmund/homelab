---
# Utilities Stack - n8n, ChangeDetection.io, Copyparty

- name: Create {{ stack_name }} directories
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ ansible_user }}"
    mode: '0755'
  loop:
    - "{{ docker_base_path }}/{{ stack_name }}"
    - "{{ docker_base_path }}/{{ stack_name }}/env"
    - "{{ docker_base_path }}/{{ stack_name }}/config/changedetection"
    - "{{ docker_base_path }}/{{ stack_name }}/config/copyparty"
    - "{{ docker_base_path }}/{{ stack_name }}/config/copyparty/hist"

# feederhub's distroless image runs as uid 65532; the data dir
# needs to belong to that uid so the SQLite DB can be opened.
- name: Create feederhub data directory
  file:
    path: "{{ docker_base_path }}/{{ stack_name }}/config/feederhub"
    state: directory
    owner: "{{ feederhub_data_uid }}"
    group: "{{ feederhub_data_gid }}"
    mode: '0750'

- name: Deploy compose file
  template:
    src: compose.yaml.j2
    dest: "{{ docker_base_path }}/{{ stack_name }}/compose.yaml"
    owner: "{{ ansible_user }}"
    mode: '0644'
  notify: Restart {{ stack_name }} stack

- name: Deploy environment files
  template:
    src: "env/{{ item }}.env.j2"
    dest: "{{ docker_base_path }}/{{ stack_name }}/env/{{ item }}.env"
    owner: "{{ ansible_user }}"
    mode: '0600'
  loop:
    - n8n
    - n8n-db
    - feederhub
  no_log: true
  notify: Restart {{ stack_name }} stack

- name: Set copyparty directory ownership
  file:
    path: "{{ docker_base_path }}/{{ stack_name }}/config/copyparty"
    state: directory
    owner: "{{ puid }}"
    group: "{{ pgid }}"
    recurse: yes

- name: Deploy copyparty config
  template:
    src: config/copyparty.conf.j2
    dest: "{{ docker_base_path }}/{{ stack_name }}/config/copyparty/copyparty.conf"
    owner: "{{ puid }}"
    group: "{{ pgid }}"
    mode: '0644'
  notify: Restart {{ stack_name }} stack

# Vane has no published image of our fork; clone the repo so docker
# compose can build it in-place. force: false keeps any local tweaks;
# bump vane_repo_version to pull a new revision on the next deploy.
- name: Clone Vane source for in-place build
  ansible.builtin.git:
    repo: "{{ vane_repo_url }}"
    dest: "{{ docker_base_path }}/{{ stack_name }}/vane-src"
    version: "{{ vane_repo_version }}"
    force: false
    update: true
  notify: Restart {{ stack_name }} stack

- name: Deploy {{ stack_name }} stack
  community.docker.docker_compose_v2:
    project_src: "{{ docker_base_path }}/{{ stack_name }}"
    state: present
    remove_orphans: true
    pull: "{{ docker_pull_policy | default('missing') }}"
    # `policy` lets docker compose decide: build any service with a
    # build: directive when the local image is missing. Required for
    # the in-place Vane build. Remove the image (or bump
    # vane_repo_version then `docker compose build --no-cache vane`)
    # to force a rebuild on subsequent deploys.
    build: always
  register: compose_result
