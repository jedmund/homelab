---
- name: Restart Degoog after extension configuration
  community.docker.docker_compose_v2:
    project_src: "{{ config_base }}"
    services:
      - degoog
    state: restarted
  when: not ansible_check_mode
